[package]
name = "sqube-agent-guard"
version = "1.0.3"
edition = "2021"
description = "Execution authorization for AI agents (v1 contract tests; guard aligns with v0.1 core)"
license = "Apache-2.0"
repository = "https://github.com/Sqube-Groups/sqube-agent-control"
readme = "README.md"
keywords = ["ai", "agents", "execution", "guardrail"]

[lib]
name = "sqube_agent_guard"

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1", features = ["v4"] }
regex = "1"
thiserror = "1"

[dev-dependencies]
tempfile = "3"
